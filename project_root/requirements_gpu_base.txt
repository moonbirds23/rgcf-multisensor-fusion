# Base dependencies for this project on the GPU machine.
# Install PyTorch separately first according to your CUDA driver.
#
# Recommended order:
#   1) install torch GPU wheel
#   2) pip install -r requirements_gpu_base.txt

numpy==1.22.0
scipy==1.10.1
pandas==2.0.3
scikit-learn==1.3.2
matplotlib==3.7.5
pillow==10.4.0
joblib==1.4.2
tqdm==4.66.5
